# External Unity Assets

Large original Unity source asset packs live under `unity_imports/` locally and
are intentionally not committed to this GitHub repository.

The runtime Unity project promotes selected assets into `Assets/TouhouMigration`
as migration slices need them. Keep the full source packs in external storage,
Git LFS, or a release artifact if they need to be shared later.
