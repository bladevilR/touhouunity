# Current Handoff — Resume Point

Last updated: 2026-06-26 (session 6 close)

## ▶▶ RESUME HERE (session 6 → 7)

**State:** my work all pushed; `origin/main` at **`b1452e6`**. Regression **91/91**. Working tree has **only the concurrent session's 4 uncommitted files** dirty (see ⚠️ note) — I never touched them; stage by path only.

**Session 6 shipped 34 verified slices** — a complete **finite-shop-stock** feature line (1-5), a **seed-bag gacha** + crop-rarity foundation (6-7), and a **functionally COMPLETE E6 cardbuild Cirno card-fight** (8-31): the boss-mechanic units (8-15), the integration spine (16-19), the card-play loop (20-26 — effect parsing, `play_card` orchestration, runtime-requirement gate, seconds-based cooldown, terrain-suppression), and **the full per-card `_apply_cirno_card_resolution`** (27-31 — all four archetypes fire/blood/mechanism/mokou + the melt-the-lake domain-seal combo). **A real card plays end-to-end through its bespoke resolution.** E6 — the deprioritized "biggest remaining depth" — is now logic-complete. All on top of the concurrent session's uncommitted tree, all collision-safe (Economy/Farming/Foundation/Save/CardBuild only), TDD red→green, regression green throughout:
  1. `f4de29a` **finite stock ledger** — `MigrationShopStock` (Godot `ShopManager.runtime_stock`): per-shop/per-item stock seeded from the catalog, decrements on buy ("out_of_stock" gate), refunds carried items on sell-back, daily `ResetShop`. `MigrationShop` takes an optional ledger (null = unlimited, existing callers unaffected). +`ShopStockSmokeTests`.
  2. `20cf93b` **festival_items parsing** — the orphan `festival_items` block (flower_festival/moon_festival/new_year/tanabata) was silently dropped; now parsed + `GetFestivalItems()`, mirroring seasonal.
  3. `122685f` **seasonal/festival merge** — `MigrationShopStock.MergeStock(shopId, items)` layers a season's/festival's items onto a shop's stock so they're buyable in-season.
  4. `42e677e` **save persistence** — `ShopStockSnapshot` (JsonUtility-safe) + `MigrationSaveData.shop_stock` + orchestrator 14th optional dep; stock now survives reopen AND save/load.
  5. `a08d94a` **day-start refresh** — `MigrationDayCycle` re-seeds shop stock each new day (Godot `refresh_all_stock`) via two optional deps.
  6. `0eed1d8` **crop rarity/season** — `MigrationCropDefinition` now parses Rarity (CropRarity) + Season (CropSeason) from crops.json + `CanPlantIn` (Godot `can_plant_in_season`); `MigrationCropDatabase` exposes `GetCropsByRarity` + `CanPlantInSeason`. Foundation for the gacha.
  7. `75876b8` **seed-bag gacha** — `MigrationSeedBag` ports Godot `ShopManager.open_seed_bag` + `_roll_{bamboo,silver,gold}_bag`: a bag opens into 3 seeds rolled by rarity (in-season crops weighted x3), `crop_<x>`→`seed_<x>`, with silver/gold pity counters. RNG injected → deterministic + unit-tested. **Blocked-on next:** wiring buy→open→inventory (Godot's `is_seed_bag` purchase branch) needs **seed item definitions** — items.json has 13 items, none `seed_*`/`seed_bag_*`, so the rolled seeds aren't addable to inventory yet. That's a data task needing author intent (which seeds/prices/stacks) — don't guess.
  8. `8f08cab` **E6 cardbuild substrate** — `MigrationCardRunState` ports Godot `CardBuildRuntimeState`'s resource + per-target status model: `AddResource`/`SpendResource`(−1 = all)/`GetResource`, `ApplyStatus`/`ConsumeStatus`(−1 = all, erases at 0)/`GetStatus` keyed per target (enemy/player). The substrate card resolution builds on. (Deck piles already exist in `MigrationCardDeck`; field objects / boss clauses / Mokou action chain are deferred.)
  9. `ee432db` **E6 cost gating** — `MigrationCardRunState.MissingCostResource(cost)` ports Godot `_missing_cost_resource`: first unaffordable resource id (positive reqs only) or "". The data-independent kernel of `play_card_by_id`'s blocking check.
  10. `9432406` **E6 boss HP** — `MigrationCardBossHp` ports the boss HP pool (CIRNO_MAX_HP 540 / `_damage_boss` / `is_boss_defeated`): clamped damage (no heal/no-op when non-positive or defeated), returns HP removed.
  11. `e7155c9` **E6 guard multiplier** — `MigrationCardBossHp.ApplyPlayerAttack` ports `apply_player_attack_damage`: resolves a raw amount through the Cirno guard model (x1.0 + 0.16/rewritten-rule when vulnerable; else x0.42 at terrain pressure ≤1, otherwise x0.18), clamps ≥1, applies it. Vulnerability/terrain/rewritten-rule are passed in.
  12. `04c315f` **E6 vulnerability window** — `MigrationCardVulnerabilityWindow` ports `_vulnerability_seconds`/`tick`/`is_vulnerability_open`: extend-only `Open`, `Reduce`, per-frame `Tick` countdown, `IsOpen` (> 0.05s). Feeds `ApplyPlayerAttack`'s `vulnerabilityOpen` input.
  13. `3308ebd` **E6 boss-clause lifecycle** — `MigrationCardBossClauses` ports `CardBossClauseController` (+ the clause storage): install → reveal → expose → seal (only exposed + matching answer family + ≥2 cards), plus disable-turns countdown. `IsSealed` backs the vuln window's deferred clause-sealed disjunct.
  14. `22150e2` **E6 boss-domain contest** — `MigrationCardBossDomains` ports `CardBossDomainController`: contest progress → threshold (break+seal), +1 answer-tag / +1 answer-family bonus (family = tag prefix before `:`/`/`), sealed/zero-amount guards, independent pressure ticks.
  15. `76bb057` **E6 Mokou charge/release** — `MigrationMokouChargeState` ports the charge core of `CardBuildMokouActionChain`: build to 100 (full at 1.6s) / overshoot to 150 with overheat self-damage (HP floored at 1); release gates on bound terminal + phase + energy, scales base damage by the charge tier (0.45/0.65/0.85/1.0) × overheat/bonus × 0.2 perfect bonus, applies flame. (The chain's dodge/ignite/process-modifier/trigger surface is a follow-up.)
  16. `bef66da` **E6 run-controller facade** — `MigrationCardBuildRunController` composes the split units behind one object (the Unity answer to Godot's `CardBuildRuntimeState` god-object) and resolves the composite queries: `IsVulnerabilityOpen` = window-open OR boss-clause-sealed (completing the slice-12 disjunct), and `ApplyPlayerAttack` resolved through the run's own terrain/rewritten/vulnerability state.
  17. `1fca9e0` **E6 count-based deck ops** — `MigrationCardDeck.{Discard,Exhaust,Retain}FromHand(int count)` (Godot `discard/exhaust/retain(amount)`, pop_back), what the effect executor dispatches to.
  18. `a6e485b` **E6 effect executor** — `MigrationCardEffectExecutor` ports `CardEffectExecutor`: dispatches a card's effect blocks against the facade (create/spend_resource, apply/consume_status, draw/discard/exhaust/retain, reveal/seal_clause), returning the ignored-type list. `MigrationCardEffectBlock` models a block; `MigrationCardBossClauses.Seal` added (direct seal).
  19. `bb137c6` **E6 effect collections** — facade summons/installed/field/partner/bullet append-lists + executor dispatch.
  20. `eeb4f6d` **E6 Mokou process modifiers** — `MigrationMokouChargeState.ApplyProcessModifier` (dodge-retain max / charge-speed multiply / damage-bonus add, consumed by the charge math) + `InstallTrigger`.
  21. `f127323` **E6 full effect-type coverage** — widened `MigrationCardEffectBlock` (mokou/modify_bullet payloads); executor now dispatches `mokou_bind_terminal`/`mokou_process_modifier`/`mokou_trigger` into `run.Mokou` and treats `damage`/`self_damage` as handled no-ops. Only unknown types remain ignored — full `CardEffectExecutor` parity.
  22. `4de7275` **E6 effect_blocks parser** — `MigrationCardEffectBlockParser` reads cards.json via dict-based `MigrationJson` (absent fields stay null) → `MigrationCardEffectBlock` lists per card_id. Real card data drives the engine.
  23. `8e41b0c` **E6 play_card orchestration** — `MigrationCardBuildRunController.PlayCard` ports the generic `play_card_by_id` flow (gate on boss-defeated/in-hand/cooldown[new `Deck.IsOnCooldown`]/cost → run effect blocks → discard → cooldown), `CardPlayResult`.
  24. `e716049` **E6 runtime-requirement gate** — `RuntimeRequirementReason` ports `_runtime_requirement_disabled_reason`; `PlayCard` consults it.
  25. `dc20e3b` **E6 seconds-based cooldown** — Cirno's `_card_cooldowns` (seconds) on the facade (`SetCardCooldown`/`GetCardCooldown`/`IsCardOnCooldown`>0.05/`TickCardCooldowns`/`ReduceCardCooldowns`), distinct from the deck's turn-based pile; `PlayCard` gates on it (settled the cooldown-model decision).
  26. `8073057` **E6 terrain-suppression + pressure clamp** — `_terrain_suppression_seconds`/`is_terrain_suppressed` (window OR clause-sealed) + `AddTerrainPressure` clamped to [0,6].
  27. `3983d96` **E6 resolution: fire archetype** — `ResolveCardEffect` resolves the 12 fire cards (ember shot, ash collector, detonation palm, ash-seal clause-seal, hourai-phoenix burst, …); `PlayCard` runs it after the effect blocks.
  28. `bbf6bcd` **E6 resolution: blood archetype** — 12 blood cards (scarlet shot, fate spear, gungnir burst, night dash, break destiny, …).
  29. `7c2e361` **E6 resolution: mechanism archetype** — 12 mechanism cards (seal economy, binding/fantasy verdict, clause lock, legal trap, …).
  30. `b6e98b5` **E6 resolution: mokou archetype** — fire bird, fujiyama burst, flame fist, hourai doll, xu-fu dimension, honest-man's-death, …
  31. `ca6d9bd` **E6 resolution: melt_the_lake** — the domain-contest → domain-seal → boss-clause-seal combo, completing `_apply_cirno_card_resolution` (all 40+ cards).
  32. `df02b81` **E6 Mokou dodge surface** — `RegisterDodge` (retain charge × dodge-retain; perfect → +energy/+ember), `Ignite` (consume flame for 2.5 dmg each), `TakeDamage` (ash accrual 1/20 + immortal-gauge revive to 35 HP), `ResolveHitTrigger`. `CardBuildMokouActionChain` now fully ported.
  33. `01cb57b` **E6 run setup** — `SetupCirnoRun` installs the boss clause + threshold-3 domain (exposed, the `terrain_tyranny` answer families); facade default clause id is the faithful `CIRNO_CLAUSE_ID`. The vulnerability / terrain-suppression / melt-the-lake clause-seal paths now fire in a real run.
  34. `b1452e6` **E6 cooldown durations** — `CardCooldownDuration` (Godot `CARD_COOLDOWNS`, 24 entries, 3.0s default); `PlayCard` applies a card's own replay cooldown automatically.

**E6 forward — the Cirno card-fight is LOGIC-COMPLETE; remaining is BLOCKED on data/concurrent-file:** the entire boss-fight engine + card-play orchestration + all 40+ cards' resolution + the full Mokou action chain + run setup + cooldown durations are done & tested (slices 8-34). The remaining E6 pieces are blocked, not pure-logic:
  1. ⚠️ **Card costs (DATA DECISION)** — cards.json has no top-level `cost` dict (only `charge_action.energy_cost` per-terminal). `PlayCard` takes cost as an optional param; confirm the card-cost source before feeding real costs (don't invent).
  2. ⚠️ **Scene/MonoBehaviour wiring** — a `CirnoBossArena` driver (build a `MigrationCardBuildRunController`, tick it, bind card-play UI to `PlayCard`) belongs in `TouhouMigrationProjectBuilder.cs` — **one of the concurrent session's 4 uncommitted files**; do it once that lands/clears.
  3. Thin/overlapping: a profile→run factory (deck-seeding from `CardBuildProfile` already exists in `MigrationCardBuildRun`).

**The shop-stock line is logic+integration complete** (ledger / festival / merge / save / day-refresh, all as capability+slot). **The ONE remaining piece is owner-wiring** — `MigrationGlobalUiController` must: construct a single shared `MigrationShopStock`, `InitializeFrom(shopDatabase)` at startup, build each `MigrationShop` with it, merge the active season's `GetSeasonalItems` after reset, pass it as the day-cycle's `shopStock`+`shopDatabase` and the orchestrator's 14th arg, and surface it to the shop UI (stock display + sold-out). **This is BLOCKED:** `MigrationGlobalUiController.cs` is one of the concurrent session's 4 uncommitted files (see ⚠️). Do the owner-wiring once those land/clear.

**Why I stopped here:** the clean self-contained pure-logic well that avoids the 4 concurrent files is now essentially dry — every remaining thread hits a wall that isn't mine to push through. Remaining work is: (a) shop-stock **owner-wiring** + seed-bag **buy→open wiring** (the first blocked on the concurrent `MigrationGlobalUiController` edits; the second blocked on missing `seed_*` item definitions — a data task), (b) **E6 cardbuild** — surveyed this session: the Cirno `CardBuildMvpRunController` (1283 lines) is deeply boss-bespoke (ember/fate/seal resources, burn/fate_lock statuses, domains, terrain pressure) and the Unity cards.json (12 Mokou cards) has no generic top-level `cost` dict, so there's **no clean quick generic slice** — it's a deliberate multi-slice real-time port, (c) **Codex/image2 art** (portraits/textures/audio/VFX/models), (d) **user decisions** (Git LFS for the 4×52 MB terrains; E4 uGUI/UITK production-UI reskin vs. keep IMGUI), (e) **ambiguous data** (NPC roster Chinese-name→canonical id reconciliation — needs author intent, don't guess).

**Open question for session 7:** pick a direction —
  1. **Continue E6 cardbuild** (now in motion) — keep porting the Cirno controller per the "E6 forward" order above; the largest remaining pure-logic depth.
  2. **Shop-stock + seed-bag owner/data wiring** — unblock by (a) the concurrent `MigrationGlobalUiController` edits landing/clearing, then owner-wire the shop ledger + UI, and (b) deciding the `seed_*`/`seed_bag_*` item definitions so the gacha is purchasable.
  3. Standing user decisions: **Git LFS** (history rewrite — risky while a 2nd session shares the tree) and the **uGUI/UITK production-UI** reskin.

---

## (session 5 close)

Last updated: 2026-06-26 (session 5 close)

### ▶▶ (historical) RESUME (session 5 → 6)

**State:** clean, all pushed, in sync with `origin/main` at **`f0a5fd8`**. Regression **74/74**; 29 scenes play-validated 0 errors.

**Session 5 shipped 10 verified slices** (all on top of `9e0b7ea`): fishing interactor `6d6c90e` · shop modal+interactor `763cf3d` · portrait hook `49cc8c8` · save position/scene `3ade62c` · audio catalog+manager `6643b0a` · NPC reachability (7 home locations) `d24ab07` · SFX triggers `1529218` · farming yield-by-quality `66353dd` · fertilizer action `1a638cf` · seasonal shop items `98aad1a` · handoff `a95ea60`/`f0a5fd8`. (Two of the log commits — `6744192` camera, `4b7dd91` Mokou-rig — are the **concurrent session's**, see the ⚠️ note below.)

**Why I stopped:** the clean non-art / non-blocked / pure-logic well is largely mined out. Remaining work is gated on a **user decision** or **Codex/image2 art** or is a **large reskin / ambiguous data task** (see "Remaining" list lower down). I asked the user to pick the next focus; **they switched conversations before answering** — so the open question for session 6 is *which direction to prioritize*:
  1. **Finite shop stock** refactor (persistent runtime shop registry: stock decrements on buy, survives reopen, seasonal stock merged in-season) — self-contained, collision-safe, testable.
  2. **E6 cardbuild** (un-deprioritize) — the Cirno `CardBuildMvpRunController` (~1280 lines) is the biggest remaining depth.
  3. **Reconcile NPC roster ids** (best-effort Chinese-name→canonical mapping) to unblock hour-driven NPC schedules — needs user OK on a few guesses.
  4. Keep mining small pure-logic slices (diminishing returns).
  Plus two standing user decisions: **Git LFS** for the 4×52 MB terrains (history rewrite — extra-risky while a 2nd session shares the tree), and whether to invest in the **E4 uGUI/UITK production-UI** reskin vs. keep the working IMGUI shells.

---

Last updated: 2026-06-25 (session 5)

## Session 5 (production phase — life-sim consumers + E5 hook, all green, all pushed)

Continuing autonomously toward "complete all migration." Three committed+pushed slices on top of `9e0b7ea`:

- **E4 fishing consumer (`6d6c90e`): `MigrationFishingSpotInteractor`.** Mirrors the bed/farm-plot interactor pattern (proximity + interact key → owner-exposed `Fishing` service → weighted catch into inventory). Placed on a dock+water prop in Misty Lake via a new optional `decorate` hook on `CreateNatureLocationScene` (existing callers unaffected). Fishing-level boost passes 0 (TODO until player progression tracks it). Service logic already TDD-covered; interactor exercised by play-validation.
- **E4 shop consumer (`763cf3d`): `MigrationShopController` (scoped IMGUI modal) + `MigrationShopInteractor`.** Modal mirrors `MigrationGiftSelectionController` (Bind/OpenForShop/Close/IsOpen + OnGUI): a shopkeeper interactor opens it for a shop id; it draws that shop's catalog with buy/sell buttons against the live `MigrationShop` (open-hours + per-shop price/buy_rate gated, coins via progress service). Owner wiring: builds a `MigrationShopService`, binds the controller (hour from world clock), gates `OpenShop` like `OpenCooking`, includes it in `BlocksGameplayInput` + Escape-close. Builder adds the controller to the GlobalUI object and places `town_general`/`town_blacksmith` interactors on the two shop props in every village scene. New `ShopControllerSmokeTests`.
- **E5 portrait hook (`49cc8c8`): `MigrationPortraitCatalog` + dialogue portrait slot.** Resolves a speaker (npc id + expression) → Resources key `Portraits/<npc>/<expression>` with normalization/fallback grounded in the dialogue data (neutral/happy/sad/surprised/angry; mad→angry, upset→sad, surprise/shocked→surprised; unknown→neutral; blank speaker = narration). `RuneDialogueController` resolves `PortraitResourceKey`+`PortraitTexture` per line and renders it in the reserved left region, **drawn only when the texture exists so layout is unchanged until art lands**. Per the division of labor the **PNGs are Codex/image2's job** — `Assets/TouhouMigration/Resources/Portraits/<npc>/<expression>.png`, documented in `Resources/Portraits/README.md` (35 npc ids × 5 expressions). New `PortraitCatalogSmokeTests`.

- **E8 save parity complete (`3ade62c`): player position + active scene.** The last caller-owned gap. `MigrationGlobalUiController.SaveGame` captures the active scene name + player transform position into `MigrationSaveData` (fields already existed); `LoadGame` restores the position when the saved scene matches the active one. `MigrationPlayerLocation` holds the pure scene/position mapping (Write + same-scene gate), `PlayerLocationSaveSmokeTests`. **Save parity is now comprehensive end to end.** Cross-scene restore (load saved scene → place player) is a scene-flow follow-up.
- **E7 audio hook (`6643b0a`): `MigrationAudioCatalog` + `MigrationAudioManager`.** Godot AudioManager port. Catalog resolves a BGM track / scene BGM / (multi-variant) SFX name → a Resources key (BGM_TRACKS/SCENE_BGM/SFX_MAP), UnityEngine-free + unit-tested. Manager is a **null-safe** playback shell: looping BGM AudioSource that auto-plays the active scene's track on Start + name-addressed one-shot SFX with pitch jitter. The owner creates it **at runtime** (like the other services — no per-scene serialization) and exposes it as `Audio`. **Clips are Codex/image2's job** — `Resources/Audio/BGM/<name>` + `Resources/Audio/SFX/<cat>/<name>`, documented in `Resources/Audio/README.md`. `AudioCatalogSmokeTests`.
- **E5 reachability (`d24ab07`): canonical residents in 7 home locations.** New `CreateLocationNpcCast` spawns a location's residents as `MigrationNpcInteractor` markers (reusing the gift/dialogue/bond wiring): HakureiShrine (reimu/sanae/suika), ScarletMansionFront (remilia/sakuya/patchouli/meiling), DungeonEntrance/Chireiden (satori/koishi/utsuho/rin), MagicForest (marisa/alice/kogasa), MistyLake (cirno/mystia), MokouHouse3D (mokou), BambooHouse/Eientei (kaguya/eirin/reisen/tewi). 21 markers; ids match `_npc_*.json`.

- **E7 SFX triggers (`1529218`):** the owner fires null-safe SFX at the seams it mediates — `ui_open` on shop/cooking/gift modals, `bond_up` on a successful gift, `coin` on a successful shop buy/sell (the shop controller got an optional `sfx` callback to stay decoupled). Closes the audio loop end-to-end.
- **E4 farming depth (`66353dd` + `1a638cf`):** resolved the open yield-design decision — `MigrationFarmingManager.Harvest` scales the crop's Min/Max range roll by the plot's water/fertilizer quality multiplier (Normal 1.0× → Legendary 3.0×, Godot `_calculate_harvest_yield`), keeping the range model intact (Normal plots unchanged). Then made it reachable: `MigrationFarmingManager.Fertilize` + a fertilize key (G) on the farm-plot interactor so the player can push a plot to high quality. Two new manager tests.
- **E4 seasonal stock (`98aad1a`):** `MigrationShopDatabase` parses `seasonal_items` (Godot shops.json) and exposes `GetSeasonalItems(season)` (spring/summer/autumn/winter) via a shared `ParseItemList`. New test. (Surfacing seasonal stock in the shop UI + finite stock decrement are follow-ups.)

**Verified:** regression **74/74** (was 70; +ShopController +PortraitCatalog +PlayerLocationSave +AudioCatalog, plus added cases to FarmingManager/ShopDatabase suites); 29 scenes play-validated 0 errors throughout. Tree clean, all pushed.

> ⚠️ **Concurrent session note (2026-06-25/26):** a second Claude session worked this **same working directory** in parallel and committed `6744192` (third-person follow camera + camera-relative movement) and `4b7dd91` (Mokou rigged as an animated Humanoid — solves the E1.5 visual blocker). Two agents in one tree means: (1) **never `git add -A`** — stage only your own files by path; (2) only one Unity batch can hold the project lock, so guard each batch (`ps … grep batchmode` → abort/retry) and don't kill the other's run; (3) `git checkout -- <generated dirs>` to drop `BuildInitialProject` churn, but check `git status` first so you don't revert the other session's in-flight files (they were editing `TouhouMigrationProjectBuilder.cs`, `MigrationGlobalUiController.cs`, `MigrationLocomotionAnimatorBridge.cs`, `ScenePortal.cs`).

**Division-of-labor pattern reused twice (portraits, audio):** when a feature needs generated art (images/audio), Claude builds the catalog/manager + a Resources-key slot + a README listing exactly what Codex/image2 must drop in, with every load null-safe so scenes run clean before the art lands. The same recipe applies to remaining E7 (textures/VFX/character models).

**Remaining (next candidates):** finite shop **stock decrement** (needs a persistent runtime shop registry so stock survives reopen — more than a one-liner) + surfacing seasonal stock in the shop UI; jingle SFX on quest-complete/level-up/item-obtained (needs events plumbed through the services — no clean owner seam yet); **E5 hour-driven NPC schedule placement**, blocked on the `human_village_roster.json` Chinese-display-name ids → canonical `_npc_*.json` ids reconciliation (a flagged data task needing author intent — don't guess); E4 uGUI/UITK production-UI reskin of the IMGUI shells; E7 textured materials / VFX / more character models (Codex/image2 art) + the 4×52 MB terrain Git LFS decision (needs user greenlight); E6 cardbuild (still deprioritized). **The clean non-art, non-blocked pure-logic well is running low** — most remaining work is art (Codex), a user decision (LFS), a large UI reskin, or an ambiguous data task.

**Reusable seam learned this session:** rebuilding location scenes via `BuildLocationScenes` re-serializes **every** scene non-deterministically (symmetric full-file churn). Only the scene(s) you actually changed show a net content delta — keep just those, `git checkout --` the rest (the same hygiene as the `BuildInitialProject` generated-dir revert).

**Life-sim scene-interactor layer is now broad:** cooking, NPC (dialogue/gift), bed (sleep), farm-plot (plant/water/harvest), **fishing (cast)**, **shop (buy/sell)**. Remaining E4 is the uGUI/UITK production-UI reskin of the IMGUI shells (deferred). Next candidates: **E8 player position/scene save scalars** (completes save parity), **E5 NPC spawning into the remaining locations + hour schedules**, **E7 audio manager hooks** (mirror the portrait division-of-labor: wire the manager + clip-path slots, stub the audio art for Codex).

## (session 4)
Last updated: 2026-06-25 (session 4)

## Objective & Ownership

Build the full Touhou Phantom game in `/Users/Shared/TouhouUnityMigration`, migrating from Godot
(`/Users/Shared/Touhougodot`, read-only reference). Claude is sole owner (Codex stopped 2026-06-25).
Standing goal: **complete all migration**, driven autonomously, user reviews final results only.

**▶ CURRENT FOCUS:** migrate all formal game scenes (E3) — **DONE (session 4): all 29 formal scenes are built Unity-native + play-validated runtime-clean, and the canonical NPC cast is spawned into Human Village.** The user's explicit top priority (实装所有场景) is complete at blockout grade. **Next per roadmap (E6 cardbuild stays deprioritized): E4 life-sim closure (shop/cooking/farming/fishing UIs + day-loop into the scenes), E5 dialogue breadth (portraits, all 35 NPCs), E7 presentation (URP/audio/VFX/textured materials + Godot-faithful location fidelity), E8 save/content parity + final QA.** Session-1 "asset promotion blocker" resolved (E3.2).

- North-star roadmap: `Docs/superpowers/plans/2026-06-25-migration-completion-roadmap.md` (Phase 0 → E1…E8; all locations incl. PureNature/AngryMesh variants are in scope).
- GitHub: `git@github.com:bladevilR/touhouunity.git` (`main`). Latest commit: `ed9b170` (session-4 farm-plot interactor). **E3 complete (29 scenes; NPCs in Human Village + 4 villages); E4 day-loop LIVE + fully gameplay-wired (sleep/fatigue/memory/farm-plot interactors); E8 save comprehensive (7 services + calendar) owner-wired.** 46 commits this session, all pushed; regression 70/70; 29 scenes play-validated 0 errors.

> 🎨 **DIVISION OF LABOR (user, 2026-06-25): image generation is Codex's job (it has `image2`).** For any feature needing generated images — NPC **portraits** (E5), **textures** for the blockout→fidelity pass (E7), UI art/icons, sprite/VFX art — **leave the image asset stubbed and wire only the code/data hooks** (portrait-path field, material slot, texture ref), marking the seam (`// TODO(codex/image2): …`). Build all the non-image parts so the generated image drops into a ready slot. Don't source/create/import art.

## Verified State

- Tree clean, `main` in sync with origin. ~158 runtime C# files; **70 smoke-test suites** — **last full regression 70/70 green (session 4)**. (session 3 added E6.4 cardbuild-run, H1 home-storage, F1 fatigue, S1 npc-memory, M1 meta-progression, S3 npc-relationship, EV1 event-manager, C1 companion-roster; extended the card-deck + farm-plot + npc-memory + event-manager suites).
- Last full regression: **60/60 green** (`MigrationSmokeTestRunner.RunAll`, session 2) — through the E6.2 card-deck-fidelity slice; 0 compile errors. (Session-2 added suites: E5.2 dialogue-humanity, E4.1 shop-service, E4.2 farm-plot, E4.3 shop-hours, E5.3 dialogue-bond, E5.4 story-flag, E5.6 time-of-day, E4.4 farming-manager, E4.5 fishing, E4.7 npc-schedule, E4.8 npc-manager, E4.9 crop-database, E4.10 shop-database, E4.11 shop-runtime, E3.1 npc-roster, E4.14 fish-database, E6.1 card-deck (60 total; E5.7's moon-phase suite was removed by the E5.8 correction); E2.4/E8.1/E8.2 extended `GameStateRulesSmokeTests`/`SaveOrchestratorSmokeTests`/`StoryFlagServiceSmokeTests`; E2.5/E5.5/E5.8/E5.9/E4.15 are owner-only wiring gated by `GlobalUiSmokeTests` + play-mode.)
- **26 scenes play-validated runtime-clean** via `MigrationPlayModeValidator` (session 4: Scenes 26, Failed False, 0 errors each) — Bootstrap/BambooHome/HumanVillage/TitleScreen + 6 PureNature variants + AngryMesh + MagicForest + MistyLake + 4 villages (TownWorld/FantasyVillage/Suntail×2) + 4 landmarks (HakureiShrine/ScarletMansionFront/DungeonEntrance/Farm) + 2 bamboo variants (MokouHouse3D/BambooHouse) + 3 arenas (CombatArena/HD2D/CirnoBoss). 4-scene baseline was re-confirmed at the end of session 2 (Scenes: 4, Failed: False, 0 errors each) with all session-2 owner changes active (menu-mode `Update` polling, world-time scaling, `ModeChanged` subscription, humanity/story-flag context wiring, **+ E4.15 life-sim catalog loading** — crops/shops/fish/roster load runtime-clean in `Awake`). (Note: it can't capture Screen-Space-Overlay UI, so UI scenes screenshot black — a capture limitation, not a game error.)
- Completion: **~30% by roadmap structure.** This is a multi-session, weeks-scale effort. (E3 locations: **all formal registry locations now have a play-validated Unity scene** — 25 location scene files. Remaining E3: main/entry flow scenes (MainMenu/LoadingScreen/World) + spawning NPCs into the locations. Then E4-E8 systems/closure. Note: location scenes are **blockout-grade** Unity-native — real promoted env meshes (nature) or Suntail/primitive structures with flat materials; E7 swaps in textured materials + Godot-faithful layouts.)

## Done (session 4 — E3 scene migration, asset/scene work)

**E3.2 (`e5c407b`): `PureNatureMeadows` location scene — first E3 scene + proven pipeline.** This is asset/scene work (not the pure-logic TDD rhythm). It establishes the reusable template that unblocks all remaining locations:
- **Promotion:** copied 26 curated raw `.fbx` meshes (Oak/Birch/Willow/Elm/Cypres/Bush, Stone/Cliff rocks, mushrooms, grass/flowers, mountains) + the 1000m terrain `.obj` from `ExternalUnityAssets/unity_imports/PureNature_Meadows` → `Assets/TouhouMigration/Art/Locations/PureNatureMeadows`. **Copy raw meshes only** (no source `.meta`/`.import`/textures → Unity regenerates GUIDs); apply flat project-owned URP materials (Human Village pattern). Sidesteps the prefab dependency-chain problem.
- **Builder:** `CreatePureNatureMeadowsScene` + helpers in `TouhouMigrationProjectBuilder`. Key reusable techniques: terrain **origin-normalized** so the spawn column rests at y=0 regardless of the terrain's hills (`CenterGroundOnPlane` + a raycast at origin); props **auto-scaled** to a target height via `NormalizeVisualBounds` (no need to know each mesh's native scale) and **raycast-grounded** onto the terrain collider (`SampleGroundY`, graceful fallback; needs `Physics.SyncTransforms()` after adding the terrain collider); a **low establishing follow camera** (skybox + horizon mountains visible) — the proven top-down framing washed out a flat meadow into fog.
- **Focused builder entry:** new `Touhou Migration/Build Location Scenes` menu method authors location scenes **without** the ~44-asset enemy-prefab churn of `BuildInitialProject`. (Note: the full smoke regression still calls `BuildInitialProject`, so still `git checkout --` the generated dirs after a regression — see "How To Work".)
- **Registration:** `MigrationSceneId.PureNatureMeadows` + `MigrationSceneCatalog` case + `MigrationSceneRegistry` (`pure_nature_meadows` flipped to available) + `RegisterBuildScenes` (EditorBuildSettings) + the validator's `ScenePaths`.
- **Verified:** build 0 compile errors; play-validation 5 scenes / 0 errors (meadow renders as a place — grove, shadows, terrain, horizon mountains, sky, portal, player); full regression 68/68.
- **Deferred (E7 polish):** real PureNature textured materials (flat colors now), per-submesh trunk/leaf split, terrain splatmap material, denser scatter, gameplay/NPC content.
- **Next:** fan the template out — the other 5 PureNature variants (`PureNature_1_2`/Classic, Jungle, Islands, Mountains, FantasyForest) reuse the exact same flow (promote that pack's meshes → a `Create<Variant>Scene` builder method → register → validate).

**E3.3 (`34778ef`): the other 5 PureNature variant locations — Classic, Jungle, Islands, Mountains, FantasyForest.** Generalized the meadow recipe into a config-driven `CreatePureNatureVariantScene` that **auto-discovers** promoted meshes per category folder (`AssetDatabase.FindAssets("t:Model", …)`) and places them via the shared `LocationGroveLayout` (the validated meadow placement) — so each pack ships whatever species it actually has, regardless of differing folder names/structures (handled Classic's `Vegetation` folder, FantasyForest's `Mushroom`, Mountains' `Mountains`, etc.). Each variant: own promoted terrain OBJ + curated FBX under `Art/Locations/<Variant>`, per-biome flat materials, registered across the data files + build settings + validator. **All 6 PureNature overworld variants now ship**, all play-validated runtime-clean (10 scenes total, 0 errors). The generic method + `LocationGroveLayout` are the reusable engine for the remaining E3 locations.

⚠️ **Repo-size debt surfaced here (decision for the user):** promoted terrain OBJs are large ASCII exports — 4 of the PureNature terrains are **52 MB each** (~364 MB in `Art/Locations` already). GitHub warns past 50 MB/file (pushes still succeed under the 100 MB hard limit). Across the remaining ~24 locations this will bloat the repo to 1 GB+. **Recommended fix: adopt Git LFS for `*.obj`/`*.fbx`/`*.glb`** — but that needs `git lfs migrate` + a force-push to rewrite history, which the roadmap forbids doing autonomously (§7 "never force-push shared history"). Left for the user to greenlight. Cheaper interim option for future locations: downsample/decimate terrain meshes before promoting, or prefer the packs' smaller terrain reps.

**E3.4 (`a87a9b7`): AngryMesh Meadow location.** Reused the generic builder for the AngryMesh pack. Since AngryMesh ships **no terrain export**, generalized the builder to take an explicit terrain path — empty → a large flat-ground `Plane` (its built-in MeshCollider keeps prop raycasts working) → **no new 50 MB terrain bloat**. Renamed `CreatePureNatureVariantScene` → `CreateNatureLocationScene` (now the engine for any nature pack, incl. the upcoming bespoke MagicForest/MistyLake). Promoted 28 AngryMesh meshes (Trees/Flowers/Rocks/Mushrooms/Background) into the standard category layout. AngryMesh trees are higher-fidelity than PureNature. Registered + validated (11 scenes, 0 errors); regression 68/68. (Added a registry option `angrymesh_meadow` "草甸·野境".)

**E3.5 (`e718ff4`→`2497139`): the rest of the formal locations — every registry location now ships.** Built on the generic builders from E3.2–E3.4:
- **MagicForest + MistyLake** (`e718ff4`): generic nature builder on flat ground, distinct palettes (dark teal woods / blue-grey misty water). Updated `GlobalUiSmokeTests` (magic_forest now selectable).
- **4 villages** (`3fd7f2a`): `CreateVillageLocationScene` places Suntail house prefabs + shop/well/cart/bridge props + trees on flat ground (shared Suntail meshes, per-location materials). TownWorld, FantasyVillage, SuntailVillagePlayable, SuntailVillageImported.
- **4 landmarks** (`c705e51`): `CreatePrimitiveBlock` blockouts — HakureiShrine (red torii + hall + lanterns), ScarletMansionFront (twin-tower red mansion), DungeonEntrance (cliff + cave mouth + boulders), Farm (tilled plot rows + crop cubes + Suntail barn/fences).
- **Bamboo variants + arenas** (`2497139`): MokouHouse3D + BambooHouse reuse the Bamboo Home glb; CombatArena/HD2D/CirnoBossArena are walled flat arenas (dummies, or an icy Cirno marker + ice pillars). Full Perfect Freeze encounter wiring stays E6.
- **Registry**: all `MigrationSceneRegistry` options flipped available (+ added fantasy_village, suntail_village_*, combat_hd2d, mokou_house3d, bamboo_house). `GlobalUiSmokeTests` now asserts no option remains disabled. 26 scenes play-validated; regression stayed 68/68 throughout.
- **Deferred**: FarmingView/FarmPlotNode (farming sub-scenes — covered by Farm + E4); main/entry flow scenes (MainMenu/LoadingScreen/World — next); NPC spawning into locations (E3.1 roster exists; map `res://` model paths → spawn); E7 visual fidelity (textured materials, Godot-faithful per-location layouts, the 4×52 MB terrain LFS decision).

**E3.6 (`04e0148`): canonical NPC cast spawned into Human Village.** `CreateHumanVillageNpcMarkers` now spawns 15 canonical Touhou NPCs (ids matching the `_npc_*.json` dialogue/gift/bond systems, Chinese display names, per-NPC preferred gifts) in a ring around the market square, replacing the 3 hardcoded markers. marisa/reimu/keine keep their canonical loved gifts (magic_crystal/green_tea/history_book) per `SocialGiftSmokeTests`. **Finding:** the E3.1 roster JSON (`human_village_roster.json`) uses display-name ids (Chinese) that don't map to the canonical NPC ids — so markers use the canonical cast for system integration; reconciling the roster→canonical id mapping is a future data task. Real character models (VRM/glb via UniVRM/MToon) + spawning into the other locations + schedule-driven (hour) placement are E5/E7 follow-ups. 29 scenes validated; regression 68/68.

**▶ E3 IS COMPLETE (session 4).** All 6 E3 tasks done: pipeline proof → PureNature ×6 → AngryMesh → bespoke locations (villages/landmarks/bamboo/arenas) → main/entry (MainMenu/LoadingScreen/World) → NPC cast. Reusable builders in `TouhouMigrationProjectBuilder`: `CreateNatureLocationScene` (auto-discovers promoted meshes per category + `LocationGroveLayout`), `CreateVillageLocationScene`, `CreatePrimitiveBlock`/`FinishLocationScene` (landmarks), `CreateFlatGround`, `EnsureAssetFolder`, `SampleGroundY`/`CenterGroundOnPlane` (grounding). All location scenes are **blockout-grade** — E7 swaps in textured materials + Godot-faithful layouts.

## Done (session 4 — E4 day-loop closure, pure-logic TDD, all green)

After E3, started closing the life-sim loop with testable orchestrators (no Unity scene work):
- **Day cycle (`dd18fc4`→`fe44648`): `MigrationDayCycle` (`Runtime/Foundation`).** Subscribes to `GameClock.DayStarted` and fans the per-day work to the already-logic-complete services — **farming `AdvanceDay`, quest `ResetDailyQuests`, bond `StartNewDay`, NPC-memory `DecayAllMemories`, weather `UpdateForDate`** — for every new day (sleep OR natural midnight). `Sleep()` advances the clock exactly one day to `WakeHour` (firing all of the above) + `fatigue.SleepFullRecovery()`. All 7 deps optional/null-safe. Closes the handoff's "GameClock/day-start not wired", "sleep day-loop", and S2 "memory-decay day-tick driver" gaps. `MigrationDayCycleSmokeTests` (6).
- **Fatigue driver (`62478e5`): `MigrationFatigueDriver` (`Runtime/Player`).** The accrual half — adds activity fatigue each game hour (`GameClock.HourChanged`) via the Godot per-hour rates, scaled by `CurrentActivity` (Idle/Active/Farming/Mining). Pairs with the day-cycle's sleep recovery to form the full fatigue loop. `MigrationFatigueDriverSmokeTests` (4).
- **Owner-wiring DONE (`c90ed64`):** `MigrationGlobalUiController` now instantiates all 6 new life-sim services + (at end of `Awake`, once `worldSimulation`/farming resolve) constructs the `MigrationDayCycle` + `MigrationFatigueDriver` bound to the live world clock/weather, rebuilds the save orchestrator with the clock (comprehensive 7-service + calendar save wired into `SaveGame/LoadGame`), `Detach`es on `OnDestroy`, and exposes a public `Sleep()` + `FatigueDriver`. **The day-loop runs LIVE in every gameplay scene** (per-hour fatigue accrual + per-day resets); play-validated 29 scenes 0 runtime errors. **Gameplay triggers — DONE:** gift→memory (`69d4b85`), mode→fatigue-activity (`3c01d8b`), dialogue-choice→memory (`e5b336a`), and the **bed interactor** (`c8d7b4b`: `MigrationBedInteractor` in the bamboo-home variants → `owner.Sleep()`). **The life-sim day-loop is now fully playable + wired + saved.** Bed now in the canonical BambooHome too (`1cf5e6c`). NPCs spawned into the 4 village locations (`3671aba`, distinct 5-NPC subsets) — reachable beyond Human Village. (combat/quest→memory hooks aren't cleanly wireable: `QuestDefinition` has no giver NPC + combat is player-vs-enemy.)

**▶ The clean pure-logic + integration + scene-population migration is comprehensively DONE.** Life-sim consumers (scene interactors) are now partly in: cooking station, NPC (dialogue/gift), bed (sleep), **farm-plot (`ed9b170`: plant/water/harvest, growth visual)**. What remains is the **production UI/asset/presentation phase** (asset/UI-framework-bound; the deferred "production pass"):
- **E4**: remaining scene interactors (**shop**, **fishing spot** — mirror the farm-plot/bed interactor pattern: a prop + interactor → the owner-exposed service); production UIs (uGUI/UITK shop/cooking/farming/fishing replacing the IMGUI shells).
- **E5**: portrait catalog + dialogue presentation. **(Portraits are images → stub for Codex/image2; Claude builds the catalog loader + the path/slot hooks.)**
- **E7**: URP pipeline/post + audio (BGM/SFX) + VFX + the blockout→textured-material/Godot-faithful fidelity pass. **(All the textures/materials art → Codex/image2; Claude wires material slots + the Git LFS terrain decision needs user greenlight.)**
- **E6** (deprioritized): cardbuild/combat breadth.

Reusable interactor pattern (for shop/fishing): a MonoBehaviour in `Runtime/<area>` with proximity (`FindGameObjectWithTag("Player")`) + `KeyCode` input, `ResolveGlobalUi()` → call an owner-exposed service (the owner exposes `FarmingManager`/`FatigueDriver`; add more getters as needed), respecting `globalUi.BlocksGameplayInput`. Placed by `TouhouMigrationProjectBuilder` in the relevant scene via `BuildLocationScenes` (no enemy-prefab churn). See `MigrationBedInteractor`/`MigrationFarmPlotInteractor`.

**E8 save parity (`d3cd74b`→`2ac9325`): 4 more services now persist — fatigue, calendar, companion roster, home storage.** Extended `MigrationSaveOrchestrator` with optional params (existing 6-arg callers unaffected): `MigrationFatigueSystem`→`fatigue` (+`LoadFatigue`), `GameClock`→`MigrationCalendarSnapshot` (via `SetDate`+`SetTime`), `MigrationCompanionRoster`→`CompanionRosterSnapshot` (recruited+active), `MigrationHomeStorage`→`HomeStorageSnapshot` (stored items). Plus `MigrationMetaProgression`→`MetaProgressionSnapshot` (currency + upgrade levels) and `MigrationNpcRelationshipNetwork`→`NpcRelationshipSnapshot` (relationship values + faction membership + reputations; the predefined relationship catalog is rebuilt at startup, not persisted). Plus `MigrationNpcMemorySystem`→`NpcMemorySnapshot` (per-NPC memories + relationship aspects + impression; notable memories rebuilt from restored weights). **Save parity is now comprehensive — all 7 stateful services persist** (fatigue, calendar, companion, home, meta, NPC-relationships, NPC-memory) on top of the prior inventory/cooking/cooking-buffs/bonds/quests/HP/humanity/story-flags. All additive — Godot `save_schema` stays 3 (the `SaveInventorySmokeTests` parity assertion still passes); each is round-trip-tested in `SaveOrchestratorSmokeTests` (orchestrator now 13-arg, params 7-13 optional/null-safe). **Owner-wiring deferred:** the owner's `SaveGame/LoadGame` should pass the live services (fatigue/clock/companion/home/meta/relationship/memory) into the orchestrator (currently 6-arg). Player scalars (coins/level/kills) already persist via `MigrationPlayerProgressService.ApplyToSave/LoadFromSave`. **Remaining save: just the player position/scene scalars (caller-owned).**

## Done (session 3 — pure-logic TDD slices, all additive, all green)

5 grounded slices, each red→green + full regression (60→63 suites), committed + pushed:
- **E6.3** (`3c631ad`): `MigrationCardDeck` retain/exhaust/cooldown piles — completes Godot `CardDeckController` method parity (RetainFromHand / ExhaustFromHand / MoveRetainedToHand / PutOnCooldown / TickCooldowns; `max(1, turns)` clamp; expired cooldown → discard; exhaust never reshuffles).
- **E6.4** (`11398dd`): `MigrationCardBuildRun` — runtime deck seeded from `CardBuildProfile.ActiveDeck` + opening-hand draw (mirrors `CardBuildMvpRunController._apply_run_profile` layered path) + per-turn cycle (EndTurn discards hand + ticks cooldowns; StartTurn returns retained + draws). **Note:** the Cirno `CardBuildMvpRunController` (1284 lines of boss-specific real-time resolution) is the remaining E6 depth and is intentionally *not* started (depth-heavy area; chose breadth instead).
- **E4.16** (`c5d1ff2`): `MigrationFarmPlot` water/fertilizer levels (0-100, daily decay 5 / 1.5) + crop quality tiers (`CropQuality`) + scaled `CalculateHarvestYield` (Godot `FarmPlot._calculate_harvest_yield`). **Additive** — manager API + its tests untouched. Deferred: soil-memory / spirit-crystal / mutant-seed / full-moon bonuses (Masterwork+), multi-harvest regrow, water-level growth-speed coupling.
- **H1** (`4cc7b4b`): `MigrationHomeStorage` — bamboo-home storage box (Godot `HomeInteractionSystem` store/retrieve/capacity-200). New `Runtime/Home` area. Sleep/tea/meal/read interactions are signal/scene-coupled and deferred.
- **F1** (`1e9b7ed`): `MigrationFatigueSystem` (`Runtime/Player`) — player fatigue stat (Godot `FatigueSystem`): accrual/clamp 0-100, Tired/Exhausted/Collapse bands at 60/80/100, sleep/rest recovery, exhausted/collapse latches. Previously-unported autoload. Unblocks the deferred Home interactions. Accrual drivers (TimeManager/mode signals), SignalBus warnings, and the collapse→teleport-home cutscene are deferred.
- **S1** (`c16a5d4`): `MigrationNpcMemorySystem` (`Runtime/Social`) — NPC memory core (Godot `NPCMemorySystem`): `AddMemory` forms weighted positive/negative memories (per-NPC capacity evicts the weakest), shifting relationship aspects (trust/affection/respect/familiarity) and recomputing the impression (Unknown..Hostile). Deferred: save, dialogue modifiers, notable-memory queries, signals.
- **S2** (`f73ed9d`): NPC memory **decay** (Godot `_decay_npc_memories`) — per-NPC `Personality` (capacity + forgiveness/gratitude rates); `DecayAllMemories` fades weights (base 2.0; positive `/= gratitude`, negative `*= forgiveness`) and forgets memories below the min weight (5). Grateful NPCs keep good memories longer; unforgiving NPCs hold grudges longer. Deferred: the day-tick driver wiring.
- **S3 + S4** (`8ad09b6`, `4783d4b`): `MigrationNpcRelationshipNetwork` (`Runtime/Social`) — the NPC-to-NPC social graph (Godot `NPCRelationshipNetwork`). S3: order-independent pair keys, `GetRelationshipType`/`Name`, `AreFriends`/`AreEnemies`, `GetRelationshipValue`/`ModifyRelationship` (seeds at 50 on first touch — faithful Godot quirk — clamped ±100). S4: faction membership (`SetNpcFaction`/`GetNpcFaction`/`AreSameFaction`/`GetFactionMembers`, `NoFaction=-1`) + faction reputation (default 50, clamp 0-100). Deferred: the predefined `NPC_RELATIONSHIPS`/`NPC_FACTIONS`/`FACTION_INFO` data, faction names, gossip, group events, player-bond reactions.
- **S5** (`f6ad90d`): NPC memory **dialogue modifier** (Godot `get_dialogue_modifier`) — `GetDialogueModifier(npc)` derives a `GreetingStyle` from impression + a `TrustLevel` from trust + special/avoid topics from notable/negative memories (adds an optional `Topic` to the memory context). Connects the memory system to dialogue. (S1's dialogue-modifier deferral is now done.)
- **S6** (`e67457c`): NPC memory **query API** (Godot `get_notable_memories`/`recall_first_meeting`/`get_memories_of_type`) — `GetNotableMemoryCount`, `HasFirstMeetingMemory`, `GetMemoryCountOfType`. The memory system's remaining deferrals are now just save/load + the day-tick decay driver + dialogue-context wiring.
- **EV1 + EV2** (`a9ebf2b`, `f5f129b`): `MigrationEventManager` (`Runtime/Narrative`) — event-status lifecycle (Godot `EventManager`): Inactive→Active→Completed/Failed (cancel→Inactive); `TriggerEvent` gated on not-active/not-completed; per-event progress; exclusive story event (`StartStoryEvent`/`EndStoryEvent`/`HasActiveStoryEvent`). EV2 adds the **trigger cooldown** via an injected seconds clock (300s window, `force`/story bypass; `GetCooldownRemaining`/`ClearCooldown`; no clock = no cooldown). Distinct from `MigrationStoryFlagService` (fired-flags). Deferred: random-event pool, per-event data, signals.
- **C1** (`a7e9a03`): `MigrationCompanionRoster` (`Runtime/Social`) — companion recruitment + single-companion party (Godot `CompanionSystem`): `Recruit`/`IsRecruited`/`GetAllRecruited`, `AddToParty`/`RemoveFromParty`/`IsInParty`/`ActiveCompanionId` (party holds one; `MAX_COMPANIONS_IN_PARTY=1`). Deferred: combat stats/skills/HP, recruitment-condition (bond-level) checks, schedule conflicts, signals.
- **M1** (`b573f9f`): `MigrationMetaProgression` + `MigrationMetaUpgrade` (new `Runtime/Progression`) — between-run upgrade economy (Godot `MetaProgressionManager` + `MetaProgressionData.MetaUpgrade`): in-memory currency + per-upgrade levels; affordability, geometric cost (`base*mult^level`, -1 at max), linear effect, `PurchaseUpgrade`, `GetTotalBonus(effectType)`. Deferred: the concrete upgrade catalog (`MetaProgressionData.initialize`), save (Godot uses `GameSaveManager`), signals.

**Open design decision (surfaced, not actioned) — farming harvest yield amount:** the Unity `MigrationFarmingManager` rolls a random range from the crop def (`MinYield`/`MaxYield`, loaded from crops.json, covered by `TestYieldRollHonorsRange`), whereas Godot `FarmPlot.harvest()` returns the deterministic plot formula (base 4 × water/fert condition × quality). E4.16 ported the plot formula as a tested capability but did **not** rewire the manager payout (doing so would break the deliberate Min/Max range model + 2 tests). Decide: switch payout to the plot formula, keep the range, or combine — then wire `MigrationFarmPlot.CalculateHarvestYield` / `QualityTier` into `MigrationFarmingManager.Harvest` + `MigrationHarvestResult`.

## Done (session 1)

- **Phase 0 (complete):** M58 closed; git + GitHub live; first in-editor play-mode validation; master one-command test runner (`MigrationSmokeTestRunner`); save orchestration (`MigrationSaveOrchestrator`).
- **E1 player core:** i-frames (`MigrationPlayerHealthRuntime` opt-in window + owner sets 0.75s); dash (`MigrationDashState` + controller wiring, LeftControl; also fixed jump to use cooking boost); swim (`MigrationSwimState`); locomotion (`MigrationLocomotion` + controller `CurrentLocomotion` seam). **Remaining:** Mecanim humanoid AnimatorController asset + a driver pushing `CurrentLocomotion`; animation-event attack windows; split the controller's concerns. **E1.5 blocker found (session 2):** the MokouValidation clips are already Humanoid (`animationType: 3`), but `Art/Characters/Mokou/Models/mokou.glb` imports via the glTF ScriptedImporter (glTFast) → **no Mecanim Humanoid avatar**, so the humanoid clips can't retarget onto Mokou. Unblock first — give the model a Humanoid avatar (convert `mokou.glb`→FBX, or source a humanoid-rigged Mokou FBX) — then author the controller + driver. Driver logic is otherwise ready: `MigrationLocomotion.Resolve` already yields Animator-facing params.
- **E2:** `MigrationGameStateMachine` + `MigrationGameStateMode` + `MigrationGameStateRules`; owner Pushes `Dialogue` mode on dialogue start / Pops on finish (`MigrationGlobalUiController.GameState`). **World-time now gated by mode (E2, session 2):** `MigrationGameStateRules.WorldTimeScale(mode)` (0 freezes Menu/Dialogue/Cutscene, Sleeping fast-forwards ×12, else ×1) + `WorldSimulationBehaviour.SetExternalTimeScale`; the owner applies it on `gameState.ModeChanged`, so the existing Dialogue Push/Pop now actually freezes/resumes the world clock. **Menu mode now driven too (E2.5):** `SyncMenuGameState()` pushes/pops `Menu` from `unifiedMenuController.IsOpen`, so opening the pause menu freezes world-time + marks the HUD hidden via the rules. **Remaining:** drive Combat/Sleeping push/pop from gameplay (combat enter/exit, sleep); have HUD/input actually consume the rules (`BlocksGameplayInput` still reads overlay state directly); scene-flow + full sleep day-loop.
- **E5.1/E5.2:** dialogue give/take-item routing (`DialogueEffectRouter.BindInventory`, wired in owner). **`humanity` fx now routed** (E5.2, session 2) via `HumanityService` (`Runtime/Player/`, default 100, clamped 0..100, Godot `MokouMonologueSystem` level thresholds), bound in owner + read live in `BuildDialogueContext`. **bond + humanity both routed now**, plus **cross-NPC `bond_<npc>` effects (E5.3, session 2)** (e.g. `bond_keine` raised while talking to Koishi → `bondService.AddBondPoints("keine", …)`). The give_item capability is added but the current data may not use it. **`event` dialogue fx now routed too (E5.4, session 2)** via `MigrationStoryFlagService` (`Runtime/Narrative/`, `MarkEvent`/`HasEvent`), bound in owner. **All known dialogue fx are now routed** (bond, `bond_<npc>`, humanity, quest/start/complete, counter, unlock_npc, quest_progress, give/take_item, event). **Story flags + bond + humanity now drive dialogue conditions (E5.5):** `BuildDialogueContext` feeds `seen_events` (for `event`/`event_not_seen`), and bond_level + humanity are already live in the context. **`time_of_day` + `is_full_moon` + `weather` now live in the context (E5.6/E5.8/E5.9)** — **every implemented dialogue condition** (bond, humanity, time_of_day, is_full_moon, weather, active/completed/started quests, seen_events) is now driven by live world state. **Next E5:** portraits; all 35 NPCs reachable; shop dialogue flows.
- **E4/E8:** save orchestrator wired into owner `SaveGame(slot)/LoadGame(slot)` (5 service snapshots + HP; **humanity now persisted too — E8.1, session 2** via `MigrationSaveOrchestrator` 6th param + `MigrationSaveData.humanity`, round-trip TDD-verified; **fired story flags also persisted — E8.2** via `MigrationStoryFlagService` snapshot + `MigrationSaveData.story_flags` in the owner save/load). **Shop economy service added (E4.1, session 2):** `MigrationShopService` buy/sell against coins+inventory+price (`Runtime/Economy/`, `DefaultBuyRate 0.5`); pure service, not yet owner-wired. **Farming loop playable (E4.2 + E4.4, session 2):** `MigrationFarmPlot` growth core + `MigrationFarmingManager` (plots + crop catalog) — plant/water/`AdvanceDay`/harvest produce → inventory (`Runtime/Farming/`, injected RNG yield). **Shop open-hours added (E4.3):** `MigrationShopHours.IsOpen` (Godot `is_shop_open`, end-exclusive + wrap-around midnight). **Fishing added (E4.5/E4.6):** `MigrationFishingService` weighted catch by rarity + fishing-level boost (`Runtime/Fishing/`, Godot RARITY_WEIGHTS 50/30/15/5, rare+ gain level×2) → inventory; **15-fish catalog loaded + registered (E4.14)** via `MigrationFishDatabase` (`Data/Fishing/fish.json`). All 3 life-sim catalogs (crops/shops/fish) now load + register into their services. **Owner loads them all on startup (E4.15):** `InitializeLifeSimCatalogs` builds FarmingManager/ShopDatabase/FishingService/NpcManager in `Awake` (play-mode verified runtime-clean), exposed via owner properties for future scene/UI consumers. **NPC schedules + manager added (E4.7/E4.8):** `MigrationNpcSchedule.LocationAt(hour, home)` + `MigrationNpcManager` registry (`Runtime/Social/`, `LocationOf(npcId, hour)`) via the shared `MigrationHourRange` (also backs shop hours). **Crop catalog loaded (E4.9):** `MigrationCropDatabase.LoadFromPath` reads the promoted `Data/Farming/crops.json` (67 crops) → `MigrationCropDefinition`s; `MigrationFarmingManager.RegisterCropsFrom` makes them plantable by id → grow → harvest (E4.12, end-to-end). **Shop catalog loaded (E4.10):** `MigrationShopDatabase` reads the promoted `Data/Shops/shops.json` (7 shops) → `MigrationShopDefinition` (items/prices, buy_rate, open hours, owner; `GetItemPrice`/`IsOpen`/`SellsItem`). **Shop economy end-to-end (E4.11):** `MigrationShop` gates buy/sell on the shop's open hours + catalog (per-shop price + `buy_rate`), composing `MigrationShopService`. **Remaining:** build a `MigrationShop` per shop from `MigrationShopDatabase` + an NPC-interaction / shop UI + stock decrement + seasonal items; register the crop DB into a `MigrationFarmingManager` + water/quality yield scaling + day-loop/Farm-scene wiring; fishing catalog + spot/minigame wiring; NPC schedule data + Manager (hour-driven locations + scene spawns); coins/scene/position save scalars + a save-UI trigger.

## How To Work (established rhythm)

- **One Unity batch at a time** (no parallel); verify compile-green before batches.
- Unity binary: `/Applications/Unity/Hub/Editor/6000.5.0f1/Unity.app/Contents/MacOS/Unity`.
- Full regression (one command): `... -batchmode -quit -executeMethod TouhouMigration.Editor.Tests.MigrationSmokeTestRunner.RunAll`.
- Per milestone: prefer pure-logic TDD (focused smoke test, red→green); for owner/controller (MonoBehaviour) changes, gate via `GlobalUiSmokeTests` + a full regression; then **commit** (end message with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`) + **push**.
- **Builder churn:** tests that call `TouhouMigrationProjectBuilder.BuildInitialProject` regenerate ~44 generated assets (enemy prefabs/controllers, scenes). Revert before committing so commits stay focused: `git checkout -- Assets/TouhouMigration/Animations/Enemies Assets/TouhouMigration/Prefabs Assets/TouhouMigration/Scenes`. (Roadmap Phase 0.3 lists gitignoring generated assets as a deferred hygiene fix.)
- **Transient batch crashes (seen session 3):** Unity batchmode can occasionally crash natively mid-run (EXIT 139, GC/ILPP stack) or surface a spurious one-off exception in an *unrelated* generated-asset suite (e.g. `EnemyAnimationPrefabSmokeTests: Index was outside the bounds of the array`) because the crash left **partial** builder churn. Recovery: if a regression fails on a suite unrelated to your change, first confirm your own focused suite is green in isolation, then `git checkout --` the generated dirs and re-run the full regression — it goes green. Delete any leftover `mono_crash.*.json` from the repo root.
- Generated prefabs/scenes are owned by `TouhouMigrationProjectBuilder` — author them in the builder, never hand-edit generated assets. Keep the Godot source tree read-only.

## Prioritized Next (heavy integration/content — best in fresh context)

> ✅ **E3 DONE (session 4):** the user's reprioritization (*"卡牌系统往后推；优先把游戏实装的所有场景移植"*) is fulfilled — all 29 formal scenes are migrated + play-validated. The text below is retained as the historical inventory (all items now ✅). Next epics: E4/E5/E7/E8 (E6 cardbuild still deprioritized).
>
> ⚠️ **(historical) USER REPRIORITIZATION (2026-06-25, end of session 3):** *"卡牌系统往后推；优先把游戏实装的所有场景移植."* → **Scene/location migration (E3) was the TOP priority; cardbuild (E6) pushed to the back.** Work the scene inventory below first. This is asset/scene work (NOT the pure-logic TDD rhythm of session 3): promote 3D environment assets from `ExternalUnityAssets/unity_imports` → author the scene in `TouhouMigrationProjectBuilder` (never hand-edit generated scenes) → play-validate via `MigrationPlayModeValidator`. Read `Docs/UnityReplacementAndAssetStrategy.md` + `Docs/MigrationInventory.md` for the asset-promotion pipeline first.

**1. E3 — migrate ALL formal game scenes (TOP PRIORITY).** Godot entry = `scenes/main/TitleScreen.tscn`. **Status (session 4): every formal registry location now has a play-validated Unity scene (25 location scene files).** Base (Bootstrap/TitleScreen/BambooHome/HumanVillage/Mokou) + 6 PureNature variants + AngryMesh + MagicForest + MistyLake + TownWorld + FantasyVillage + Suntail×2 + HakureiShrine + ScarletMansionFront + DungeonEntrance + Farm + MokouHouse3D + BambooHouse + CombatArena + CombatArenaHD2D + CirnoBossArena. **E3 COMPLETE ✅ (session 4):** main/entry flow scenes built (MainMenu/LoadingScreen/World) + canonical NPC cast spawned into Human Village. All 29 formal scenes migrated + play-validated. Remaining scene-side polish is E7 (textured materials, Godot-faithful layouts, real NPC character models, NPC spawning into the other locations + hour-driven schedules). **Remaining formal scenes (Godot `scenes/`, skip `test/`+`dev/`+`tools/`+`debug/`+`tests/` — those are audit/dev-only, not 实装):**
   - **Main/entry (4):** `main/TitleScreen` ✅slice · `main/MainMenu` · `main/LoadingScreen` · `main/World`
   - **Locations (~22):** `BambooHouse` · `HumanVillage` (+`_Merged` / `EnvironmentSlice` / `human_village/HumanVillageCodexRemakeWorld` / `human_village/HumanVillageVerticalSlice`) · `MagicForest` · `MistyLake` · `TownWorld` · `FantasyVillage` · `SuntailVillagePlayable` / `SuntailVillageImported` · **PureNature ×6 ✅ ALL DONE** (`Classic`✅ / `Jungle`✅ / `Islands`✅ / `Meadows`✅ / `Mountains`✅ / `FantasyForest`✅ — E3.2/E3.3, session 4) · `angrymesh_meadow/AngryMeshMeadow`✅ · `hakurei_shrine/HakureiShrineCourtyard` · `scarlet_devil_mansion/ScarletDevilMansionFront`
   - **Overworld (4):** `bamboo_home/BambooHome` · `bamboo_home/MokouHouse3D` · `dungeon_entrance/DungeonEntrance` · `farm/Farm`
   - **Combat/boss (4):** `combat/CombatArena` · `combat/CombatArenaHD2D` · `bosses/cirno/CirnoBossArena` · `bosses/cirno/CirnoBossController`
   - **Farming (2):** `farming/FarmingView` · `farming/FarmPlotNode`
   - **NPC scenes (~18):** `npcs/*` (Base/Base3D + Reimu/Marisa/Sakuya/Keine/Kaguya/Koishi/Akyuu/Kosuzu/Nitori, 2D+3D) — spawnable into the locations.
   - **Already started (E3.1/E4.13):** Human Village NPC roster (26 NPCs) loaded via `MigrationNpcRoster` + `MigrationNpcManager`; next map `res://` model paths → Unity assets + spawn at each NPC's `LocationOf(npc, hour)`.
   - **Known blocker (carried from session 1) — RESOLVED in session 4 (E3.2).** The asset-promotion path works: copy raw `.fbx`/`.obj` meshes (no source `.meta`/`.import`/textures) into `Assets/TouhouMigration/Art/Locations/<Name>/`, author a `Create<Name>Scene` builder method that applies flat URP materials + auto-scales/grounds props, register the scene, validate. See "Done (session 4)" for the full recipe and reusable helpers.
2. **E2 (supports scenes):** Combat/Sleeping mode push/pop drivers + HUD/input consuming `MigrationGameStateRules`; scene-flow + sleep day-loop.
3. **E4 (mostly logic-done):** life-sim closure — shop/cooking/farming/fishing UIs + day-loop wiring into the scenes above.
4. **E5/E7/E8:** dialogue breadth (portraits, all 35 NPCs) → presentation (URP/audio/VFX/UI) → save parity + content audit + full play-validation.
5. **E6 — DEPRIORITIZED (per user 2026-06-25):** cardbuild combat breadth — Cirno `CardBuildMvpRunController` (~1280 lines), general arena, 20-enemy AI, more bosses. **Deck/run foundation done (E6.1–E6.4);** resume only after the scenes are migrated.
6. **E1.5 (asset-blocked, low priority now):** player Mecanim AnimatorController needs `mokou.glb` given a Humanoid avatar (convert→FBX or source a rigged FBX).

*(Done in session 2 — E5.2: dialogue `humanity` fx → `HumanityService`. E2.4: world-time gated by game-state mode (Dialogue/Menu/Cutscene freeze the clock, Sleeping fast-forwards). E4.1: `MigrationShopService` buy/sell economy. E8.1: humanity now persists through save/load. E2.5: opening the pause menu freezes world-time + hides HUD. E4.2/E4.4: farming loop playable (plant/water/grow/harvest → inventory). E4.3: shop open-hours. E4.5/E4.6: fishing weighted catch (+ level boost) → inventory. E4.7/E4.8: NPC schedules + manager (location by hour). E4.9: 67-crop catalog loader. E4.10: 7-shop catalog loader. E4.11: shop runtime (hours/catalog-gated buy/sell). E4.12: farming end-to-end (real crops plantable by id). E3.1: human village NPC roster (26) loaded. E4.13: NPC schedules from the roster. E4.14: 15-fish catalog → fishing service. E4.15: owner loads all life-sim catalogs on startup (play-mode verified). E6.1: CardBuild runtime deck (draw/hand/discard/reshuffle). E6.2: deck draws from front (Godot pop_front fidelity). E5.3: cross-NPC dialogue bond effects (`bond_<npc>`). E5.4: narrative `event` fx → `MigrationStoryFlagService` (all known dialogue fx now routed). E8.2: fired story flags persist through save/load. E5.5: story flags now gate dialogue (`event` conditions). E5.6: live time_of_day in the dialogue context. E5.7→E5.8: is_full_moon (corrected to the night-gated `IsFullMoonActive`). E5.9: live weather — all dialogue conditions now driven by live state. E1.5 diagnosed as asset-blocked on the Mokou humanoid avatar.)*
